// GENERATED by build_site.py - do not edit by hand.
window.TOPICS = [
  {
    "id": "taylor-series",
    "title": "Taylor / Maclaurin series",
    "subject": "Maths AA HL",
    "syllabus": "AHL 5.19",
    "folder": "math/taylor-series",
    "script": "taylor_series.py",
    "notebook": "taylor_series.ipynb",
    "demo": "taylor",
    "tags": [
      "series",
      "maclaurin",
      "taylor",
      "approximation",
      "convergence",
      "polynomial",
      "calculus",
      "sin",
      "radius of convergence",
      "estimate",
      "approximate a function",
      "why more terms help"
    ],
    "hard_bit": "You can compute the coefficients, but it is not obvious why adding terms helps, or why the approximation collapses far from the centre.",
    "shows": "The polynomial is built up one term at a time against sin(x). Each new term grips the curve over a wider interval, and outside that interval it shoots off to infinity - which is what radius of convergence means, visually.",
    "source": "\"\"\"Taylor series visualisation (IB HL Maths AA - Option/AHL: Maclaurin series).\n\nIdea: a Taylor polynomial is the \"best possible\" polynomial copy of a function\nnear one point. Adding terms widens the region where the copy is good.\n\nRun:  python taylor_series.py\n\"\"\"\n\nimport math\n\nimport matplotlib.pyplot as plt\nimport numpy as np\nfrom matplotlib.animation import FuncAnimation\n\n# --- the function we approximate, and its Maclaurin coefficients -------------\n# sin(x) = x - x^3/3! + x^5/5! - ...\nCENTRE = 0.0\nMAX_TERMS = 10\n\n\ndef taylor_sin(x, n_terms):\n    \"\"\"Maclaurin polynomial of sin(x) using n_terms non-zero terms.\"\"\"\n    total = np.zeros_like(x)\n    for k in range(n_terms):\n        power = 2 * k + 1\n        total = total + ((-1) ** k) * x**power / math.factorial(power)\n    return total\n\n\ndef main():\n    x = np.linspace(-4 * np.pi, 4 * np.pi, 800)\n    exact = np.sin(x)\n\n    fig, ax = plt.subplots(figsize=(9, 5))\n    ax.plot(x, exact, color=\"black\", lw=2, label=\"sin(x)\")\n    approx_line, = ax.plot([], [], color=\"crimson\", lw=2, label=\"Taylor polynomial\")\n    ax.axvline(CENTRE, color=\"grey\", ls=\":\", lw=1)\n\n    ax.set_ylim(-3, 3)\n    ax.set_xlim(x[0], x[-1])\n    ax.set_xlabel(\"x\")\n    ax.set_ylabel(\"y\")\n    ax.legend(loc=\"upper right\")\n    ax.grid(alpha=0.3)\n    title = ax.set_title(\"\")\n\n    def frame(i):\n        n = i + 1\n        approx_line.set_data(x, taylor_sin(x, n))\n        highest = 2 * n - 1\n        title.set_text(f\"{n} term(s) - polynomial up to x^{highest}\")\n        return approx_line, title\n\n    anim = FuncAnimation(fig, frame, frames=MAX_TERMS, interval=900, blit=False)\n    plt.tight_layout()\n    plt.show()\n    return anim\n\n\nif __name__ == \"__main__\":\n    main()\n",
    "script_url": "math/taylor-series/taylor_series.py",
    "notebook_url": "https://github.com/iyu210-tech/Capstone_Repository/blob/main/math/taylor-series/taylor_series.ipynb",
    "colab_url": "https://colab.research.google.com/github/iyu210-tech/Capstone_Repository/blob/main/math/taylor-series/taylor_series.ipynb"
  },
  {
    "id": "projectile-drag",
    "title": "Projectile motion with air resistance",
    "subject": "Physics HL",
    "syllabus": "Theme A",
    "folder": "physics/projectile-drag",
    "script": "projectile_drag.py",
    "notebook": "projectile_drag.ipynb",
    "demo": "projectile",
    "tags": [
      "projectile",
      "drag",
      "air resistance",
      "kinematics",
      "range",
      "launch angle",
      "trajectory",
      "parabola",
      "forces",
      "euler",
      "throw",
      "ball",
      "45 degrees",
      "best angle"
    ],
    "hard_bit": "Exam questions use the vacuum parabola, so it is easy to believe 45 degrees is always optimal and that the path is symmetric. Neither survives contact with drag, and there is no neat formula to show why.",
    "shows": "Real trajectories against the vacuum parabola, and range plotted against launch angle. The optimum slides below 45 degrees, and the faster you launch, the further it slides.",
    "source": "\"\"\"Projectile motion with air resistance (IB HL Physics - Topic A.1 / B).\n\nIdea: the textbook parabola assumes no drag. With a drag force proportional to\nv^2 the range shrinks, the path becomes asymmetric, and the optimal launch\nangle drops below 45 degrees.\n\nRun:  python projectile_drag.py\n\"\"\"\n\nimport matplotlib.pyplot as plt\nimport numpy as np\n\nG = 9.81          # m s^-2\nMASS = 0.145      # kg  (a baseball)\nDRAG_K = 0.0013   # drag coefficient lumped: F_drag = k * v^2, in N s^2 m^-2\nDT = 0.001        # s\n\n\ndef simulate(speed, angle_deg, drag_k):\n    \"\"\"Euler integration of the trajectory until the projectile lands.\"\"\"\n    theta = np.radians(angle_deg)\n    vx, vy = speed * np.cos(theta), speed * np.sin(theta)\n    x, y = 0.0, 0.0\n    xs, ys = [x], [y]\n\n    while y >= 0.0:\n        v = np.hypot(vx, vy)\n        # drag acts opposite to velocity, magnitude k*v^2\n        ax = -drag_k * v * vx / MASS\n        ay = -G - drag_k * v * vy / MASS\n        vx += ax * DT\n        vy += ay * DT\n        x += vx * DT\n        y += vy * DT\n        xs.append(x)\n        ys.append(y)\n\n    return np.array(xs), np.array(ys)\n\n\ndef best_angle(speed, drag_k):\n    \"\"\"Scan launch angles and return the one giving the greatest range.\"\"\"\n    angles = np.arange(10, 81, 1)\n    ranges = [simulate(speed, a, drag_k)[0][-1] for a in angles]\n    return angles[int(np.argmax(ranges))], angles, np.array(ranges)\n\n\ndef main():\n    speed = 40.0  # m/s\n\n    fig, (ax1, ax2) = plt.subplots(1, 2, figsize=(12, 5))\n\n    # left panel: trajectories at several angles, with and without drag\n    for angle in (30, 45, 60):\n        x, y = simulate(speed, angle, DRAG_K)\n        ax1.plot(x, y, lw=2, label=f\"{angle} deg (with drag)\")\n        x0, y0 = simulate(speed, angle, 0.0)\n        ax1.plot(x0, y0, lw=1, ls=\"--\", alpha=0.6, label=f\"{angle} deg (vacuum)\")\n\n    ax1.set_xlabel(\"horizontal distance / m\")\n    ax1.set_ylabel(\"height / m\")\n    ax1.set_title(f\"Trajectories at {speed:.0f} m/s\")\n    ax1.legend(fontsize=8)\n    ax1.grid(alpha=0.3)\n    ax1.set_ylim(bottom=0)\n\n    # right panel: range vs angle, showing the optimum shifting below 45 deg\n    opt_drag, angles, ranges_drag = best_angle(speed, DRAG_K)\n    opt_vac, _, ranges_vac = best_angle(speed, 0.0)\n\n    ax2.plot(angles, ranges_vac, ls=\"--\", label=f\"vacuum (best {opt_vac} deg)\")\n    ax2.plot(angles, ranges_drag, lw=2, label=f\"with drag (best {opt_drag} deg)\")\n    ax2.axvline(45, color=\"grey\", ls=\":\", lw=1)\n    ax2.set_xlabel(\"launch angle / degrees\")\n    ax2.set_ylabel(\"range / m\")\n    ax2.set_title(\"Range vs launch angle\")\n    ax2.legend()\n    ax2.grid(alpha=0.3)\n\n    plt.tight_layout()\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    main()\n",
    "script_url": "physics/projectile-drag/projectile_drag.py",
    "notebook_url": "https://github.com/iyu210-tech/Capstone_Repository/blob/main/physics/projectile-drag/projectile_drag.ipynb",
    "colab_url": "https://colab.research.google.com/github/iyu210-tech/Capstone_Repository/blob/main/physics/projectile-drag/projectile_drag.ipynb"
  },
  {
    "id": "maxwell-boltzmann",
    "title": "Maxwell-Boltzmann and activation energy",
    "subject": "Chemistry HL",
    "syllabus": "Reactivity 2.2",
    "folder": "chemistry/maxwell-boltzmann",
    "script": "maxwell_boltzmann.py",
    "notebook": "maxwell_boltzmann.ipynb",
    "demo": "boltzmann",
    "tags": [
      "maxwell-boltzmann",
      "activation energy",
      "rate of reaction",
      "kinetics",
      "arrhenius",
      "temperature",
      "catalyst",
      "distribution",
      "heat",
      "heating",
      "temperature rise",
      "why heating speeds up reactions",
      "collision"
    ],
    "hard_bit": "The curve is usually drawn once, so 'a 10 K rise roughly doubles the rate' stays a memorised fact rather than something you can see.",
    "shows": "Raising the temperature barely moves the peak - it stretches the tail past Ea. Drag the temperature slider and watch the shaded area multiply while the curve itself hardly shifts.",
    "source": "\"\"\"Maxwell-Boltzmann distribution and activation energy\n(IB HL Chemistry - Reactivity 2.2 rates of reaction).\n\nIdea: temperature does not shift the whole curve past Ea - it stretches the\ntail. A small rise in T multiplies the fraction of molecules above Ea, which is\nwhy rates roughly double per 10 K.\n\nRun:  python maxwell_boltzmann.py\n\"\"\"\n\nimport matplotlib.pyplot as plt\nimport numpy as np\nfrom scipy import integrate\n\nK_B = 1.380649e-23   # J K^-1\nN_A = 6.02214076e23  # mol^-1\nMASS = 0.028 / N_A   # kg per molecule (nitrogen, 28 g/mol)\n\nEA_KJ = 50.0                       # activation energy in kJ/mol\nEA_J = EA_KJ * 1000 / N_A          # per molecule\nV_EA = np.sqrt(2 * EA_J / MASS)    # speed with kinetic energy Ea\n\n\ndef distribution(v, T):\n    \"\"\"Maxwell-Boltzmann speed distribution f(v) for a given temperature.\"\"\"\n    a = MASS / (2 * K_B * T)\n    return 4 * np.pi * v**2 * (a / np.pi) ** 1.5 * np.exp(-a * v**2)\n\n\ndef fraction_above_ea(T):\n    \"\"\"Fraction of molecules with kinetic energy of at least Ea.\"\"\"\n    frac, _ = integrate.quad(distribution, V_EA, np.inf, args=(T,))\n    return frac\n\n\ndef main():\n    v = np.linspace(0, 2500, 1500)\n    temperatures = [300, 310, 400, 500]\n\n    fig, (ax1, ax2) = plt.subplots(1, 2, figsize=(12, 5))\n\n    for T in temperatures:\n        f = distribution(v, T)\n        ax1.plot(v, f, lw=2, label=f\"{T} K\")\n        ax1.fill_between(v, f, where=(v >= V_EA), alpha=0.25)\n\n    ax1.axvline(V_EA, color=\"black\", ls=\"--\", lw=1.5)\n    ax1.annotate(\n        f\"Ea = {EA_KJ:.0f} kJ/mol\",\n        xy=(V_EA, ax1.get_ylim()[1] * 0.85),\n        xytext=(V_EA + 150, ax1.get_ylim()[1] * 0.85),\n        fontsize=9,\n    )\n    ax1.set_xlabel(\"molecular speed / m s^-1\")\n    ax1.set_ylabel(\"fraction of molecules (per unit speed)\")\n    ax1.set_title(\"Maxwell-Boltzmann distribution for N2\")\n    ax1.legend()\n    ax1.grid(alpha=0.3)\n\n    # right panel: how the shaded fraction grows with temperature\n    temps = np.arange(250, 601, 5)\n    fractions = np.array([fraction_above_ea(T) for T in temps])\n    ax2.semilogy(temps, fractions, lw=2, color=\"crimson\")\n    ax2.set_xlabel(\"temperature / K\")\n    ax2.set_ylabel(\"fraction with E >= Ea (log scale)\")\n    ax2.set_title(\"Why 10 K matters\")\n    ax2.grid(alpha=0.3, which=\"both\")\n\n    f300, f310 = fraction_above_ea(300), fraction_above_ea(310)\n    print(f\"Fraction above Ea at 300 K: {f300:.3e}\")\n    print(f\"Fraction above Ea at 310 K: {f310:.3e}\")\n    print(f\"Ratio for a 10 K rise:      {f310 / f300:.2f}x\")\n\n    plt.tight_layout()\n    plt.show()\n\n\nif __name__ == \"__main__\":\n    main()\n",
    "script_url": "chemistry/maxwell-boltzmann/maxwell_boltzmann.py",
    "notebook_url": "https://github.com/iyu210-tech/Capstone_Repository/blob/main/chemistry/maxwell-boltzmann/maxwell_boltzmann.ipynb",
    "colab_url": "https://colab.research.google.com/github/iyu210-tech/Capstone_Repository/blob/main/chemistry/maxwell-boltzmann/maxwell_boltzmann.ipynb"
  }
];
